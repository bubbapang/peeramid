import './Feed.css';

export default function Feed() {
    // const ratings = useSelector(state => state.ratings); to add later

    return (
        <div className="feed-container">
            {/* {ratings.map(rating => 
                <FeedIndexItem post={rating} />
            )} */}
            <h1>Feed</h1>
        </div>
    )
}
